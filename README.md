# context
For Github Pages setup

## GitHub Pages base setup

This repository includes a minimal GitHub Pages configuration:

- Workflow: `.github/workflows/pages.yml`
- Site entrypoint: `docs/index.html`

On pushes to `main` (or manual dispatch), GitHub Actions deploys the `docs/` folder to GitHub Pages.
