body { font-family: system-ui, sans-serif; line-height:1.6; margin:0; padding:0 1rem; }
header { display:flex; justify-content:space-between; align-items:center; padding:1rem 0; }
nav ul { list-style:none; margin:0; padding:0; display:flex; gap:1rem; }
main { max-width:48rem; margin:1rem auto; padding:0 1rem; }
